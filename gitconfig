[user]
  name = Alexander Gitter
  email = contact@agitter.net
[color]
  ui = true
[push]
  default = simple
[alias]
  s = status
  pullff = pull --ff-only
  r = !GIT_NO_PAGER=1 git l -20
  ra = !git r --all
  l = "!. ~/.githelpers && pretty_git_log"
  la = !git l --all